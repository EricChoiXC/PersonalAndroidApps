# AGENTS.md

## 开发规范

本项目所有 Android 代码开发必须遵循 `docs/开发规范.md` 中定义的规范，包括但不限于：

- **命名规范**：包名（PBF 按功能分包）、类名（大驼峰）、方法名（小驼峰）、常量（CONSTANT_CASE）、字段（m/s 前缀 + 控件缩写）等
- **代码样式**：大括号风格、类成员顺序、参数排序、行长限制（100 字符）等
- **资源文件规范**：布局、图片、动画、values 等资源文件的命名规则
- **注释规范**：类注释、方法注释、块注释等
- **测试规范**：测试类命名、测试函数命名等

AI 在生成或修改 Android 代码时，应优先遵循上述规范，确保代码风格一致、可读性高、易于维护。
